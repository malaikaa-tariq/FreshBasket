import 'dotenv/config';import app from './app.js';import {connectDB} from './config/db.js';const port=Number(process.env.PORT||5000);connectDB().then(()=>app.listen(port,()=>console.log(`FreshBasket API running on http://localhost:${port}`))).catch((e)=>{console.error('Database connection failed',e);process.exit(1)});
