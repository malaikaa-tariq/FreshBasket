import type {Request,Response} from 'express';import Product from '../models/Product.js';
export async function listProducts(req:Request,res:Response){const q=String(req.query.q||'').trim();const category=String(req.query.category||'').trim();const limit=Math.min(Number(req.query.limit)||50,100);const filter:any={};if(q)filter.$or=[{name:{$regex:q,$options:'i'}},{description:{$regex:q,$options:'i'}}];if(category)filter.category=category;const products=await Product.find(filter).populate('category','name slug').sort({featured:-1,createdAt:-1}).limit(limit);res.json({products,count:products.length})}
export async function getProduct(req:Request,res:Response){const product=await Product.findById(req.params.id).populate('category','name slug');if(!product)return void res.status(404).json({message:'Product not found'});res.json(product)}
export async function createProduct(req:Request,res:Response){const{name,description,price,image,unit,stock,category,featured}=req.body;if(!name||!description||price===undefined||!image||!unit||stock===undefined||!category)return void res.status(400).json({message:'Missing required product fields'});res.status(201).json(await Product.create({name,description,price,image,unit,stock,category,featured:!!featured}))}
export async function updateProduct(req:Request,res:Response){const product=await Product.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true});if(!product)return void res.status(404).json({message:'Product not found'});res.json(product)}
export async function deleteProduct(req:Request,res:Response){const product=await Product.findByIdAndDelete(req.params.id);if(!product)return void res.status(404).json({message:'Product not found'});res.status(204).send()}
