import {Router} from 'express';import {allOrders,createOrder,myOrders,updateStatus} from '../controllers/orderController.js';import {authorize,protect} from '../middleware/auth.js';const router=Router();router.use(protect);router.post('/',createOrder);router.get('/my',myOrders);router.get('/',authorize('admin'),allOrders);router.patch('/:id/status',authorize('admin'),updateStatus);export default router;
